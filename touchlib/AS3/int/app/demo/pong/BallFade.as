package app.demo.pong{	import flash.display.Sprite;	import flash.filters.BitmapFilterQuality;	import flash.filters.BlurFilter;		/**	 * A movie clip that is represented by a rectangle	 */	public class BallFade extends Sprite	{		/**		 * Setup the rectangular movie clip with given dimensions		 * @param width Width of the wall		 * @param height Height of the number		 */		 		 var ballFadeX:Number = new Number(20);		 var ballFadeY:Number = new Number(4);		 var ballFade:Sprite;		public function BallFade():void{						ballFade=new Sprite();					ballFade.graphics.beginFill(0xFFFFFF);			ballFade.graphics.drawCircle(0,0,25);								//this.addChild(ballFade);			//ballFade.alpha=0.8;			ballFade.scaleX= .75;			ballFade.scaleY = .75;												//setBlur(10,5);									}				public function setBlur(fadeBlurX:Number, fadeBlurY:Number){						var blur:BlurFilter = new BlurFilter();			blur.blurX = fadeBlurX;			blur.blurY = fadeBlurY;			blur.quality = BitmapFilterQuality.MEDIUM;			ballFade.filters = [blur];						}				function setDimensions(width:Number, height:Number)		{			//var ball:ballSprite=new ballSprite();			//var ballFade:ballSprite=new ballSprite();			//ballFade.alpha = 0.75;			//ballFade.x=50;						//this.addChild(ballFade);						//this.addChild(ball);			// Be a rectangle			/*this.graphics.beginFill(0xFF0000, 1.0);			this.graphics.drawRect(0, 0, width, height);			this.graphics.endFill();*/		}	}}