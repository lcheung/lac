IF(OPENGL_FOUND AND GLUT_FOUND)
	ADD_EXECUTABLE(pong pong.cpp physicalmesh.cpp ${touchlib_SOURCE_DIR}/src/textures.cpp)
	TARGET_LINK_LIBRARIES(pong touchlib tinyxml ${OPENGL_LIBRARIES} ${GLUT_LIBRARIES})
	SET_TARGET_PROPERTIES(pong
			PROPERTIES LINK_FLAGS ${OPENCV_LINKDIR} LINK_FLAGS ${OPENCV_LDFLAGS})
ENDIF(OPENGL_FOUND AND GLUT_FOUND)